This release contains the first tagged release v0.1.0 which includes:

- Initial monitoring features
- Schedule fetch from Jenkins config.xml
- Build failure alerts
- Systemd unit + packaging support
- GitHub Actions CI + release workflows
